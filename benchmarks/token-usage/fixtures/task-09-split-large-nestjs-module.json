{
  "id": "task-09-split-large-nestjs-module",
  "category": "refactor",
  "description": "Existing NestJS app. User asks to split the 720 LOC UserController into smaller controllers per concern (profile, settings, sessions, notifications) without breaking the public API.",
  "user_message": "Refactor src/users/users.controller.ts (currently 720 LOC) by splitting it into focused controllers: profile.controller.ts, settings.controller.ts, sessions.controller.ts, notifications.controller.ts. The public REST routes must stay identical. Keep the existing service layer untouched. Update the module wiring and run the integration tests.",
  "expected_rules_triggered": [
    "architecture",
    "testing",
    "refactor"
  ],
  "context_size_target": "medium",
  "notes": "Refactor mode trigger. Public API preservation is an architecture concern. Should NOT trigger security or microservices."
}
